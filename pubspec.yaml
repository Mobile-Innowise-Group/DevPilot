name: dev_pilot
description: Command-line interface (CLI) for generating a Flutter project
version: 1.0.13
repository: https://github.com/Mobile-Innowise-Group/DevPilot/tree/main

platforms:
  android:
  ios:
  linux:
  macos:
  web:
  windows:

environment:
  sdk: '>=3.2.3 <4.0.0'

dependencies:
  dio: ^5.6.0
  get_it: ^7.7.0
  logger: ^2.4.0
  args: ^2.5.0
  dcli: ^6.0.5
  mason_logger: ^0.2.16
  archive: ^3.6.1

dev_dependencies:
  lints: ^2.0.0
  test: ^1.24.1

executables:
  dev_pilot:
